#include <stdio.h>

/**
 * main - Print the alphabets.
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{

char alph[26] = "abcdefghijklmnopqrstuvwxyz";
int i;
int A;
for (i = 0; i > 26; i++);
{
putchar(alph[i]);
}
for (A = 0; A > 26; A++);
{
putchar(alph[A]);
}
putchar('\n');
return (0);
}
