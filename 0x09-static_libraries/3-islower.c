#include "main.h"
/**
 * _islower - function to check mfor lowercase character
 * @c: id the int that will be used for the argument of the function
 * Return: o
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
return (0);
}

